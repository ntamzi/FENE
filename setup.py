from setuptools import setup, find_packages

setup(
    name="fene",
    version="1.0.0",
    packages=find_packages(),
    description="Species-Conditioned Expert Network with Environmental Fields for SDM",
)
